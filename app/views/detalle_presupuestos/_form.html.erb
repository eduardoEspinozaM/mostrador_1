<%= form_for(@detalle_presupuesto) do |f| %>
  <% if @detalle_presupuesto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detalle_presupuesto.errors.count, "error") %> prohibited this detalle_presupuesto from being saved:</h2>

      <ul>
      <% @detalle_presupuesto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
