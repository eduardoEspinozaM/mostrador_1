<% if @categoria.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody').append(' <%= j render "row", categoria: @categoria %>');
  $('.alert-success').html("La categoria ha sido creado con éxito").show();
  $('#categoria-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @categoria.errors.full_messages[0] %>');
<% end %>